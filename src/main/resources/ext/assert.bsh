assert( boolean condition )
{
    if ( ! condition ) {
        throw new com.funambol.lanciadelta.BeanShellAssertionException (
            "Test FAILED: "
            +"Line: "+ this.namespace.getInvocationLine()
            +" : "+this.namespace.getInvocationText()
            +" : while evaluating file: "+getSourceFileInfo()
        );
    }
}